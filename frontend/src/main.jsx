import React from 'react';
import ReactDOM from 'react-dom/client';
import WebNavigatorAgent from './WebNavigatorAgent';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WebNavigatorAgent />
  </React.StrictMode>
);
